# TODO: Look at the parts that I need in isolation and see if I can use them in my own scripts
# Write-Host "Applying winutil configuration..."
# $configPath = Join-Path -Path $PSScriptRoot -ChildPath "../../winutil.json"
# &([ScriptBlock]::Create((Invoke-RestMethod https://christitus.com/win))) -Config $configPath -Run
